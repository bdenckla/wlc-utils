# wlc-utils
Westminster Leningrad Codex utilities

All tracked Python now lives under `py/`, with repo-specific modules under `py/python_modules/`.

Run the main workflow from the repo root with:

```powershell
python py/main_wlc.py
```
